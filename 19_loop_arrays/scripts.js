// loops em arrays - uma técnica muito utilizada na programação é o loop nos arrays. Se trata de uma lista de valores, muitas vezes precisamos
// ver cada um deles.

let estados = ["SP", "RJ", "MG", "ES", "AM", "CE", "RN", "BA"];

for (let i = 0; i <= estados.length; i++) {
    console.log(estados[i]);
}

